<html>
  <head>
   {{  content_for_header}}
   {{ 'style.css' | asset_url | stylesheet_tag }}
  <script src="{{ 'scripts.js'| asset_url }}" />
  </head>
  <body>
    {% render 'header' %}
    {{ content_for_layout }}
  </body>
</html>
